name: Apply deliverable persistence refactor

on:
  push:
    branches:
      - inventsmith/full-product-build
    paths:
      - scripts/apply-deliverable-persistence-refactor.py
      - .github/workflows/apply-deliverable-persistence-refactor.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: inventsmith/full-product-build
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: python scripts/apply-deliverable-persistence-refactor.py
      - run: npm ci
      - run: npx tsc --noEmit
      - run: npx tsc -p convex --noEmit
      - run: npx vitest run src/__tests__/deliverable-persistence.regression.test.ts src/__tests__/validation-artifact-handoff.regression.test.ts
      - name: Commit refactor
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add convex/atlasWorkState.ts
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "refactor: centralize deliverable persistence decisions"
            git push origin HEAD:inventsmith/full-product-build
          fi
